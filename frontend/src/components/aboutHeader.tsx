import React from "react";

const AboutHeader = () => {
    return (
        <header>
            <nav>
                <h1>My navbar from aboutHeader</h1>
            </nav>
        </header>
    );
};

export default AboutHeader;
